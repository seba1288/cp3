public class Math1 {
   public static double greater(double x, double y){
       return Math.max(x, y);
    }
    static double pi = Math.PI;
    static double abssolute(double x) {
        return Math.abs(x);
    }
    static double sqrt(double x) {
        return Math.sqrt(x);
    }
    static double Randomizer() {
        return  (double) ((Math.random() * (1-0)) + 0);
    }
    static double random_dob = (int) ((Math.random() * (10-0)) + 0);

     public static double sinus = Math.sin(90);
    
}
